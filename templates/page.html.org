<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title>{{ page.title }} - {{ config.title }}</title>
</head>
<body>
  <p><a href="{{ config.base_url }}">← トップへ</a></p>

  <h1>{{ page.title }}</h1>
  {% if page.date %}
    <p>投稿日: {{ page.date }}</p>
  {% endif %}

  {{ page.content | safe }}
</body>
</html>

